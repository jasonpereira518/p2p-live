# 3D bus model

Place your bus model here as **`bus.glb`** for the student map 3D bus layer.

- Path used by the app: `/models/bus.glb` (served from `public/models/bus.glb`).
- Format: glTF binary (`.glb`). The model is scaled and oriented automatically (facing direction of travel).
